"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
export default function ForgotPassword(){const router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget),r=await fetch("/api/auth/request-password-reset",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({login:f.get("login")})}),d=await r.json();if(r.ok)setMessage(d.message);else setError(d.error||"Reset email could not be sent.");setBusy(false)}return <main className="auth-shell"><div className="auth-card"><div className="auth-brand"><span>S</span><div><strong>Sögur Forge</strong><small>Reset your password</small></div></div><form onSubmit={submit}><label>Username or email<input name="login" autoComplete="username" required/></label>{message&&<p className="auth-notice">{message}</p>}{error&&<p className="auth-error">{error}</p>}<button className="auth-submit" disabled={busy}>{busy?"Sending…":"Send reset link"}</button><button type="button" className="auth-secondary" onClick={()=>router.push("/")}>Back to sign in</button></form></div></main>}
