import {readFile} from "node:fs/promises";import {join} from "node:path";import {NextResponse} from "next/server";import {currentUser} from "@/lib/auth/session";import {query} from "@/lib/db";const dir=process.env.ASSET_DIR||"/app/data/assets";
export async function GET(_:Request,{params}:{params:Promise<{assetId:string}>}){const u=await currentUser();if(!u)return new NextResponse(null,{status:401});const {assetId}=await params;const a=await query<{storedName:string;mimeType:string}>(`SELECT a."storedName",a."mimeType" FROM "Asset" a JOIN "Novel" n ON n."id"=a."novelId" WHERE a."id"=$1 AND n."userId"=$2`,[assetId,u.id]);if(!a.rows[0])return new NextResponse(null,{status:404});try{return new NextResponse(await readFile(join(dir,a.rows[0].storedName)),{headers:{"Content-Type":a.rows[0].mimeType,"Cache-Control":"private, max-age=3600"}})}catch{return new NextResponse(null,{status:404})}}