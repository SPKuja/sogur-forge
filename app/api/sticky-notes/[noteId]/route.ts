import {NextRequest,NextResponse} from "next/server";import {currentUser} from "@/lib/auth/session";import {requireSameOrigin} from "@/lib/auth/request";import {query} from "@/lib/db";export async function PATCH(r:NextRequest,{params}:{params:Promise<{noteId:string}>}){if(!requireSameOrigin(r))return NextResponse.json({error:"Invalid origin"},{status:403});const u=await currentUser();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});const {noteId}=await params,b=await r.json(),color=["yellow","blue","pink","green"].includes(b.color)?b.color:"yellow";const x=await query(`UPDATE "StickyNote" s SET "body"=$3,"color"=$4,"positionX"=$5,"positionY"=$6,"width"=$7,"height"=$8,"updatedAt"=NOW() FROM "Novel" n WHERE s."id"=$1 AND s."novelId"=n."id" AND n."userId"=$2 RETURNING s."id"`,[noteId,u.id,String(b.body||"").slice(0,5000),color,Math.max(0,Math.round(Number(b.positionX)||0)),Math.max(0,Math.round(Number(b.positionY)||0)),Math.max(160,Math.round(Number(b.width)||220)),Math.max(120,Math.round(Number(b.height)||180))]);return x.rows[0]?NextResponse.json({ok:true}):NextResponse.json({error:"Not found"},{status:404})}export async function DELETE(r:NextRequest,{params}:{params:Promise<{noteId:string}>}){if(!requireSameOrigin(r))return NextResponse.json({error:"Invalid origin"},{status:403});const u=await currentUser();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});const {noteId}=await params;await query(`DELETE FROM "StickyNote" s USING "Novel" n WHERE s."id"=$1 AND s."novelId"=n."id" AND n."userId"=$2`,[noteId,u.id]);return NextResponse.json({ok:true})}