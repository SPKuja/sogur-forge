import {randomUUID} from "node:crypto";import {NextRequest,NextResponse} from "next/server";import {currentUser} from "@/lib/auth/session";import {requireSameOrigin} from "@/lib/auth/request";import {query} from "@/lib/db";
export async function POST(r:NextRequest){if(!requireSameOrigin(r))return NextResponse.json({error:"Invalid origin"},{status:403});const u=await currentUser();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});const b=await r.json(),own=await query(`SELECT "id" FROM "Novel" WHERE "id"=$1 AND "userId"=$2`,[b.novelId,u.id]);if(!own.rows[0])return NextResponse.json({error:"Not found"},{status:404});const id=randomUUID();await query(`INSERT INTO "StickyNote" ("id","novelId","chapterId","body","color") VALUES ($1,$2,$3,'','yellow')`,[id,b.novelId,b.chapterId||null]);return NextResponse.json({id,body:"",color:"yellow",chapterId:b.chapterId||null},{status:201})}