import {NextRequest,NextResponse} from "next/server";import {currentUser} from "@/lib/auth/session";import {requireSameOrigin} from "@/lib/auth/request";import {query} from "@/lib/db";async function own(id:string,uid:string){return (await query<{novelId:string}>(`SELECT p."novelId" FROM "Part" p JOIN "Novel" n ON n."id"=p."novelId" WHERE p."id"=$1 AND n."userId"=$2`,[id,uid])).rows[0]}export async function PATCH(r:NextRequest,{params}:{params:Promise<{partId:string}>}){if(!requireSameOrigin(r))return NextResponse.json({error:"Invalid origin"},{status:403});const u=await currentUser();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});const {partId}=await params;if(!await own(partId,u.id))return NextResponse.json({error:"Not found"},{status:404});const b=await r.json(),title=String(b.title||"").trim().slice(0,160);if(!title)return NextResponse.json({error:"Title required"},{status:400});await query(`UPDATE "Part" SET "title"=$2,"updatedAt"=NOW() WHERE "id"=$1`,[partId,title]);return NextResponse.json({ok:true})}export async function DELETE(r:NextRequest,{params}:{params:Promise<{partId:string}>}){if(!requireSameOrigin(r))return NextResponse.json({error:"Invalid origin"},{status:403});const u=await currentUser();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});const {partId}=await params;if(!await own(partId,u.id))return NextResponse.json({error:"Not found"},{status:404});await query(`UPDATE "Chapter" SET "partId"=NULL WHERE "partId"=$1`,[partId]);await query(`DELETE FROM "Part" WHERE "id"=$1`,[partId]);return NextResponse.json({ok:true})}