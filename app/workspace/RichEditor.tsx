"use client";import {useEffect,useRef,useState} from "react";
const esc=(s:string)=>s.replace(/[&<>]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;"}[c]!));export function legacyToHtml(v:string){if(!v)return "";if(/^\s*</.test(v))return v;return v.split(/\n{2,}/).map(p=>`<p>${esc(p).replace(/\n/g,"<br>")}</p>`).join("")}
export function plainText(v:string){if(typeof document==="undefined")return v.replace(/<[^>]*>/g," ");const d=document.createElement("div");d.innerHTML=legacyToHtml(v);return d.textContent||""}
export default function RichEditor({value,onChange,novelId}:{value:string;onChange:(v:string)=>void;novelId:string}){const ref=useRef<HTMLDivElement>(null),file=useRef<HTMLInputElement>(null),[uploading,setUploading]=useState(false);useEffect(()=>{if(ref.current&&ref.current.innerHTML!==legacyToHtml(value))ref.current.innerHTML=legacyToHtml(value)},[value]);function cmd(c:string,v?:string){ref.current?.focus();document.execCommand(c,false,v);if(ref.current)onChange(ref.current.innerHTML)}async function image(f?:File){if(!f)return;setUploading(true);const data=new FormData();data.set("novelId",novelId);data.set("file",f);try{const r=await fetch("/api/assets",{method:"POST",body:data});if(!r.ok)throw new Error();const a=await r.json();cmd("insertHTML",`<figure class="manuscript-image"><img src="${a.url}" alt=""><figcaption></figcaption></figure><p><br></p>`)}finally{setUploading(false);if(file.current)file.current.value=""}}return <><div className="formatbar" role="toolbar" aria-label="Formatting"><button onMouseDown={e=>{e.preventDefault();cmd("bold")}}><b>B</b></button><button onMouseDown={e=>{e.preventDefault();cmd("italic")}}><i>I</i></button><button onMouseDown={e=>{e.preventDefault();cmd("underline")}}><u>U</u></button><button onMouseDown={e=>{e.preventDefault();cmd("formatBlock","h2")}}>H</button><button onMouseDown={e=>{e.preventDefault();cmd("formatBlock","blockquote")}}>❝</button><button onMouseDown={e=>{e.preventDefault();cmd("insertUnorderedList")}}>• List</button><span/><button onMouseDown={e=>{e.preventDefault();cmd("justifyLeft")}}>≡</button><button onMouseDown={e=>{e.preventDefault();cmd("justifyCenter")}}>≡</button><button onMouseDown={e=>{e.preventDefault();cmd("justifyRight")}}>≡</button><button onClick={()=>file.current?.click()} disabled={uploading}>{uploading?"Uploading…":"▧ Image"}</button><input ref={file} hidden type="file" accept="image/jpeg,image/png,image/webp,image/gif" onChange={e=>image(e.target.files?.[0])}/></div><div ref={ref} className="rich-editor" contentEditable suppressContentEditableWarning spellCheck onInput={e=>onChange(e.currentTarget.innerHTML)} onDrop={e=>{const f=e.dataTransfer.files?.[0];if(f?.type.startsWith("image/")){e.preventDefault();image(f)}}}/></>}