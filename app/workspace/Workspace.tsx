"use client";
import {useEffect,useMemo,useRef,useState} from "react";import {useRouter} from "next/navigation";
type Theme="system"|"light"|"dark";type Chapter={id:string;partId:string|null;partTitle:string|null;title:string;content:string;summary:string;status:string;position:number};
export default function Workspace({username,novel,initialChapters}:{username:string;novel:{id:string;title:string};initialChapters:Chapter[]}){const router=useRouter();const [chapters,setChapters]=useState(initialChapters);const [activeId,setActiveId]=useState(initialChapters[0]?.id??"");const [focus,setFocus]=useState(false),[navOpen,setNavOpen]=useState(false),[notesOpen,setNotesOpen]=useState(false);const [theme,setTheme]=useState<Theme>("system");const [save,setSave]=useState<"saved"|"saving"|"error">("saved");const first=useRef(true);const chapter=chapters.find(c=>c.id===activeId)??chapters[0];const words=useMemo(()=>chapter?.content.trim()?chapter.content.trim().split(/\s+/).length:0,[chapter?.content]);
 useEffect(()=>{const s=localStorage.getItem("sogur-theme") as Theme|null;if(s==="light"||s==="dark"||s==="system")setTheme(s)},[]);useEffect(()=>{localStorage.setItem("sogur-theme",theme);if(theme==="system")document.documentElement.removeAttribute("data-theme");else document.documentElement.dataset.theme=theme},[theme]);
 useEffect(()=>{if(first.current){first.current=false;return}if(!chapter)return;setSave("saving");const t=setTimeout(async()=>{try{const r=await fetch(`/api/chapters/${chapter.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({title:chapter.title,content:chapter.content,summary:chapter.summary,status:chapter.status})});setSave(r.ok?"saved":"error")}catch{setSave("error")}},700);return()=>clearTimeout(t)},[chapter?.title,chapter?.content,chapter?.summary,chapter?.status,chapter?.id]);
 function update(p:Partial<Chapter>){setChapters(v=>v.map(c=>c.id===activeId?{...c,...p}:c))}
 async function addChapter(){const r=await fetch("/api/chapters",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({novelId:novel.id})});if(r.ok){const c=await r.json();setChapters(v=>[...v,c]);setActiveId(c.id);setNavOpen(false)}}
 async function logout(){await fetch("/api/auth/logout",{method:"POST"});router.push("/");router.refresh()}
 if(!chapter)return null;
 const side=<aside className={`sidebar ${navOpen?"open":""}`}><div className="brand"><span>S</span><div><strong>Sögur Forge</strong><small>{username}</small></div><button className="mobile-close" onClick={()=>setNavOpen(false)}>×</button></div><button className="project project-button" onClick={()=>router.push("/workspace")}><small>← LIBRARY</small><strong>{novel.title}</strong></button><nav><button className="active">✦ <span>Manuscript</span></button><button>♙ <span>Characters</span></button><button>⌖ <span>Locations</span></button><button onClick={()=>router.push(`/workspace/${novel.id}/cork-board`)}>▣ <span>Cork Board</span></button><button>◌ <span>Ideas</span></button><button>◇ <span>World notes</span></button></nav><div className="chapter-head"><small>CHAPTERS</small><button onClick={addChapter}>＋</button></div><div className="chapters">{chapters.map((c,i)=><button key={c.id} className={c.id===activeId?"active":""} onClick={()=>{setActiveId(c.id);setNavOpen(false)}}><small>CHAPTER {String(i+1).padStart(2,"0")}</small>{c.title}</button>)}</div><div className="sidebar-foot"><div className="goal"><div><span>Chapter words</span><strong>{words.toLocaleString()}</strong></div><i><b style={{width:`${Math.min(100,words/10)}%`}}/></i><small>Daily goal · 1,000</small></div><div className="theme-switch">{(["system","light","dark"] as Theme[]).map(v=><button key={v} className={theme===v?"active":""} onClick={()=>setTheme(v)}>{v==="system"?"Auto":v[0].toUpperCase()+v.slice(1)}</button>)}</div></div></aside>;
 return <div className={focus?"app focus":"app"}>{!focus&&side}{!focus&&navOpen&&<button className="scrim" onClick={()=>setNavOpen(false)}/>}<main>{!focus&&<header><div className="header-left"><button className="menu-button" onClick={()=>setNavOpen(true)}>☰</button><span>{novel.title} <b>/</b> <strong>{chapter.title}</strong></span></div><div className="header-actions"><span className={`session-dot ${save==="error"?"save-error":""}`}><i/>{save==="saving"?"Saving…":save==="error"?"Save failed":"Saved"}</span><button onClick={()=>setNotesOpen(true)} className="notes-button">Notes</button><button onClick={()=>setFocus(true)}>Focus</button><button onClick={logout} className="signout">Sign out</button></div></header>}<section className="manuscript"><div className="editor">{focus&&<button className="exit" onClick={()=>setFocus(false)}>Exit focus</button>}<input value={chapter.title} onChange={e=>update({title:e.target.value})}/><p className="meta">Chapter {String(chapters.indexOf(chapter)+1).padStart(2,"0")} · {words.toLocaleString()} words · {Math.max(1,Math.ceil(words/220))} min read</p><textarea spellCheck value={chapter.content} onChange={e=>update({content:e.target.value})}/></div>{!focus&&<aside className={`notes ${notesOpen?"open":""}`}><div className="notes-title"><small>CHAPTER NOTES</small><button onClick={()=>setNotesOpen(false)}>×</button></div><label>Status</label><select value={chapter.status} onChange={e=>update({status:e.target.value})}><option value="DRAFT">Draft</option><option value="REVISE">Revise</option><option value="DONE">Done</option></select><label>Summary</label><textarea value={chapter.summary} onChange={e=>update({summary:e.target.value})}/></aside>}</section>{!focus&&notesOpen&&<button className="notes-scrim" onClick={()=>setNotesOpen(false)}/>}</main></div>
}